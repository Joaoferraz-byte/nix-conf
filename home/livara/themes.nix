{
  # Theme generation and application adapters are owned by
  # inputs.shell-conf.homeManagerModules.default. Keeping this module as an
  # explicit boundary avoids a second Matugen configuration being evaluated
  # for the same user.
}
