# Retired shell records

These files preserve the audit trail of former shell experiments and troubleshooting decisions. They are historical evidence only and are not imported by the active NixOS composition.

The active architecture is documented in the repository root `ARCHITECTURE.md` and `README.md`, while the current shell contract is published by `shell-conf`.
